<?php
return array(
	'rules' => array(
		/*You can declare your own rules here like this:
		
		//This would check if the value consists of uppercase
		//letters only
		'uppercase' => function($val) {
			return (bool)preg_match('#[A-Z]#',$val);
		}
		*/
		
	)
);